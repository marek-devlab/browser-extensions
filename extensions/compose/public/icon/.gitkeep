# Icons are generated by scripts/gen-icons.mjs at the repo root, which writes
# 16/32/48/128.png into every extension's public/icon/. This extension
# (@blur/compose — "Markdown Workbench") is NOT yet wired into that script.
#
# TODO(icons): extend scripts/gen-icons.mjs to emit a compose glyph (a pencil /
# "M↓" mark on the family accent) into extensions/compose/public/icon/{16,32,48,128}.png,
# then delete this .gitkeep. Until then no PNGs are committed here and the
# manifest's action.default_icon / top-level icons will 404 in a real build.
