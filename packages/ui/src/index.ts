// @blur/ui — shared design tokens, theme plumbing, presentational primitives and
// scaffold-phase mock helpers for the extension family. Pure presentation: no
// `wxt`/browser or storage imports (cf. @blur/core = pure logic). CSS is exported
// separately: `import '@blur/ui/tokens.css'` (+ optionally `/components.css`).

export * from './theme';
export * from './components';
export * from './mock';
